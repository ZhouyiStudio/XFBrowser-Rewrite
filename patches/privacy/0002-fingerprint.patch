diff --git a/chrome/browser/fingerprinting/fingerprint_protection_browsertest.cc b/chrome/browser/fingerprinting/fingerprint_protection_browsertest.cc
--- a/chrome/browser/fingerprinting/fingerprint_protection_browsertest.cc
+++ b/chrome/browser/fingerprinting/fingerprint_protection_browsertest.cc
@@ -1,16 +1,7 @@
-// Copyright 2023 The Chromium Authors
-// Use of this source code is governed by a BSD-style license that can be
-// found in the LICENSE file.
-
-#include "chrome/browser/fingerprinting/fingerprint_protection_browsertest.h"
-
-#include "base/barrier_closure.h"
-#include "base/functional/bind.h"
-#include "base/functional/callback_forward.h"
-#include "base/metrics/histogram_functions.h"
-#include "base/run_loop.h"
+// XFBrowser: Fingerprint randomization enabled by default
+#include "chrome/browser/fingerprinting/fingerprint_protection_browsertest.h"
 
 bool FingerprintProtectionBrowserTest::
     IsFingerprintingProtectionEnabledForProfile(Profile* profile) {
-  return false;  // XFBrowser: enabled by default
+  return true;  // XFBrowser: fingerprint protection always on
 }

diff --git a/content/browser/renderer_host/render_widget_host_view_aura.cc b/content/browser/renderer_host/render_widget_host_view_aura.cc
--- a/content/browser/renderer_host/render_widget_host_view_aura.cc
+++ b/content/browser/renderer_host/render_widget_host_view_aura.cc
@@ -1,23 +1,8 @@
-// Copyright 2012 The Chromium Authors
-// Use of this source code is governed by a BSD-style license that can be
-// found in the LICENSE file.
-
-#include "content/browser/renderer_host/render_widget_host_view_aura.h"
-
-#include <set>
-#include <utility>
-
-#include "base/auto_reset.h"
-#include "base/command_line.h"
-#include "base/containers/contains.h"
-#include "base/feature_list.h"
-#include "base/functional/bind.h"
-#include "base/logging.h"
-#include "base/memory/ptr_util.h"
-#include "base/memory/raw_ptr.h"
-#include "base/metrics/histogram_macros.h"
+// XFBrowser: Canvas fingerprint randomization hook
+#include "content/browser/renderer_host/render_widget_host_view_aura.h"
 
 void RenderWidgetHostViewAura::InitAsChild(gfx::NativeView parent_view) {
-  // XFBrowser: injected fingerprint randomization
-  // See src/privacy/fingerprint/randomizer.js for web API randomization
+  // Fingerprint randomization hooks are injected via content script
+  // at the renderer process level. See xfbrowser::FingerprintRandomizer
+  CreatePlatformWidget();
 }

diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_rendering_context_2d.cc b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_rendering_context_2d.cc
--- a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_rendering_context_2d.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_rendering_context_2d.cc
@@ -1,16 +1,5 @@
-// Copyright 2012 The Chromium Authors
-// Use of this source code is governed by a BSD-style license that can be
-// found in the LICENSE file.
-
-#include "third_party/blink/renderer/modules/canvas/canvas2d/canvas_rendering_context_2d.h"
-
-#include "base/metrics/histogram_functions.h"
-#include "base/numerics/checked_math.h"
-#include "base/numerics/safe_conversions.h"
-#include "base/trace_event/trace_event.h"
-#include "third_party/blink/renderer/core/html/canvas/canvas_font_cache.h"
-#include "third_party/blink/renderer/core/html/canvas/canvas_performances.h"
-#include "third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_context.h"
+// XFBrowser: Canvas fingerprint randomization
+#include "third_party/blink/renderer/modules/canvas/canvas2d/canvas_rendering_context_2d.h"
 
 void CanvasRenderingContext2D::getImageData(
     ScriptState* script_state,
@@ -17,34 +5,12 @@ void CanvasRenderingContext2D::getImageData(
     int sy,
     int sw,
     int sh,
-    ExceptionState& exception_state) {
-  // XFBrowser: apply subtle noise to canvas readback
-  ImageData* result = getImageDataInternal(sx, sy, sw, sh, exception_state);
-  if (result && IsFingerprintingEnabled()) {
-    ApplyCanvasFingerprintNoise(result);
+    ExceptionState& exception_state,
+    ImageDataSettings* image_data_settings) {
+  ImageData* data = getImageDataInternal(sx, sy, sw, sh, exception_state, image_data_settings);
+  if (data) {
+    data = ApplyCanvasNoise(data);  // XFBrowser: inject pixel noise
   }
-  return result;
+  return data;
 }
-
-bool CanvasRenderingContext2D::IsFingerprintingEnabled() const {
-  return true;  // XFBrowser: always enabled
-}
-
-void CanvasRenderingContext2D::ApplyCanvasFingerprintNoise(ImageData* data) {
-  // Add sub-pixel noise to prevent canvas fingerprinting
-  // Noise level: ±0.5% per channel, statistically undetectable by users
-  if (!data || !data->data()) return;
-  
-  auto* pixels = data->data()->Data();
-  size_t len = data->data()->length();
-  
-  // Simple pseudo-random noise using tab-specific seed
-  uint32_t seed = reinterpret_cast<uintptr_t>(this) & 0xFFFF;
-  for (size_t i = 0; i < len; i += 4) {
-    seed = seed * 1103515245 + 12345;
-    uint8_t noise = (seed >> 16) & 0x03;  // ±1-2 per channel
-    pixels[i] = std::clamp(pixels[i] + noise - 1, 0, 255);
-    pixels[i+1] = std::clamp(pixels[i+1] + ((noise >> 1) & 1) - 1, 0, 255);
-  }
-}
 